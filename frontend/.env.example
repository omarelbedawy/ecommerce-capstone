# For local dev, leave this out entirely (vite.config.js proxy handles /api automatically)
# For deployment (Vercel), set this to your deployed backend URL + /api, e.g:
# VITE_API_URL=https://your-backend-name.onrender.com/api
